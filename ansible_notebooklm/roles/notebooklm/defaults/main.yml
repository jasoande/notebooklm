# defaults file for notebooklm
---
project_root: "{{ ansible_env.HOME }}/Venella_2026"
vars_py_path: "{{ project_root }}/vars.py"

# Local container cache settings
container_image_tag: "quay.io/jasoande/notebooklm-core-engine:latest"
container_name: "notebooklm_task_worker"

# Fallback profile variables for vars.py parsing emulation
clients:
  - merck_test
  - organon_test
  - blue_yonder_test
  - heshey_test
  - lord_abbett_test
